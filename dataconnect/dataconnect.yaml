specVersion: "v1"
serviceId: "webgues"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "webgues-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
